struct node{
    int data;
    struct node*left;
    struct node* right;

}
function to create the node 
    stuct node*createnode(int data);{
        struct node* n;
        n=(struct node*) malloc (sizeof(struct node))
        n-> data=data;
        n->left=NULL;
        n->right=NULL;
        return n;
    }

    function to count no of nodes in a binary tree
    int countnodes(struct node * root){
        if(root==NULL){
            return 0
        }
        return 1+countnodes(root->left)+countnodes(root->right)
    }

    function to count no of leaf nodes in the binary tree 
    as we know a leaf node has bothe its left and right pointer as NULL

    int countleafnodes(struct node*root){
        if(root==NULL){
            return 0;
        }
        if (root->left==NULL && root->right==NULL){
            return ;
        }
         return countleafnodes(root->left)+countleafnodes(root->right);
    }

    func to count the height of the tree 
    int height(struct node * root){
        if(root==null){
            return 0;
        }
        leftheight=height(root->left);
        rightheight=height(root->right);

        return(leftheight>rightheight ? leftheight:rightheight)+1;

    }

    preorder traversal (root->leftsubtree->rightsubtree)
    fucn for preorder traversal 

    void preorder(struct node*root){
        if(root==NULL){
            return ;

        }
        if (root!=NULL){
            printf("%d",root->data);
            preorder(root ->left);
            preorder(root ->right);
        }
    }
    postorder traversal (leftsubtree -> rightsubtree->root)
    func to implement postorder traversal 
    void postorder(struct node*root){
        if(root==NULL){
            return;

        }
        if(root!=NULL){
            postorder(root->left);
            postorder(root->right);
            printf("%d",root->data);
        }
    }

    inorder traversal (leftsubtree ->root->rightsubtree)
    func for inorder traversal 
    void inorder(struct node*root){
        if(root==NULL){
            return;
        }

        if(root!=NULL){
            inorder(root->left)
            print("%d",root->data)
            inorder(root->right)    
        }
    }
    check wether the given tree is a bst or not 
    int isbst(struct node*root){
        static struct node*prev=NULL
        if(root==NULL){
            return 0;
        }
        if(root!=NULL){
            if(!isbst(root->left)){
                return 0;
            }
            if(prev!=NULL && root->data<=prev->data){
                return 0;
            }
            prev=root
            return(isbst(root->right))

        }
    }
    searching in a bst tree 
    struct node * search(struct node*root,int key ){
        if(root==NULL){
            return NULL;

        }
        if(root==key){
            return key ;
        }
        else if(root->data > key){
            return search(root->left);

        }
        else
            return search(root->right)
    }


    #LINKED LIST 
    #include <stdio.h>
    #include<stdlib.h>

    struct Node{
        int data;
        struct Node*next;
    }
    void display(struct node*ptr){
        while(ptr!= NULL){
            printf("element:- %d",ptr->data);
            ptr=ptr->next
        }
    }
    struct Node *insertbegining(struct node *head,int data){
        struct node*ptr
        ptr=(struct *node)malloc(sizeof(struct node));
        ptr->next=head;
        head=ptr;
        return head;
    }
    struct Node *insertatindex(struct node*head,int data,int index){
        struct node*ptr1
        ptr=(struct *node)malloc(sizeof(struct node));
        struct node *p=head;
        while(int i!=index-1){
            p=p->next;
            i++
        }
        ptr->data=data
        ptr->next=p->next;
        p->next=ptr
        return head;}  

    struct node *insertatend(struct node*head,int data){
        struct node*ptr1
        prt1=(struct *node)malloc(sizeof(struct node));
        struct node *p=head;
        while(p->next!=Null){
            p=p->next
        }
        ptr->next=NULL;
        p->next=ptr;
        return head;

    }
    DELETION IN LINKED LIST 
    struct node *deletebeigning(struct node *head){
       struct node*p=head;
       head=head->next;
       free(p);
       return head; 
    }
    struct node *deletefromindex(struct node*head,int index){
        struct node*p=head;
        struct node*q=head->next
        while(i!=index-1){
            p=p->next;
            q=q->next;
            i++
        }
        p->next=q->next;
        free(q);
        return head;

    }
    struct node *deletefromend(struct node*head){
        struct node*p=head;
        struct node*q=head;
        while(p->next!=NULL){
            q=p;
            p=p->next
        }
        
        
    }  
    

    
    int main(){
        struct node*head;
        struct node*second;
        struct node*third;

        head=(struct node*)malloc(sizeof(struct node));
        second=(struct node*)malloc(sizeof(struct node));
        third=(struct node*)malloc(sizeof(struct node));

        head ->data=7;
        head->next=second;

        second->data=8;
        second->next=third;

        third->data=11;
        third ->next =NULL
        
        display(head)
    }
    return 0;

    STACK OPERATIONS
    #include <stdio.h>

    define max 100;
    int stack [max];
    int top=-1

    void push(int data)
    {
        if(top==Max-1){
            printf("stack is overflow");
            return 0;
        }
        stack[++top]=data
        printf("element pushed");
    }
    int pop(){
        if(top==-1){
            printf("stack underflow");
            
        }
        return stack[top--];

    }
    int isempty(){
        if(top==-1){
            return 1;
        }
    }

    int peek(){
        return stack[top];
    }
    void display(int stack[],int top)
    {
        if(top==-1)
        {
            printf("stack is empty");
        }
        for(int i =0, i<=top,i++)
        {
            printf("%d",stack[i]);
        }
    }
